To run:

python3 manage.py runserver

Go to localhost/main_app to check the actual application.

Input a League of Legends summoner name that is in a game and the data will populate the screen.

Waits are in place to prevent rate limiting.
